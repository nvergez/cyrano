{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-overlay",
  "description": "Capability for the recording overlay window",
  "windows": ["recording-overlay"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default",
    "notification:default"
  ]
}
